<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "My Rails Bootstrap App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "actiontext", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
      console.log("=== INLINE SCRIPT LOADED ===");
      
      document.addEventListener("DOMContentLoaded", function() {
        console.log("=== DOM READY ===");
        
        const button = document.querySelector(".theme-switcher");
        console.log("Button found:", button);
        
        if (button) {
          button.addEventListener("click", function() {
            console.log("=== BUTTON CLICKED ===");
            
            const currentTheme = document.documentElement.getAttribute("data-bs-theme") || "light";
            const newTheme = currentTheme === "dark" ? "light" : "dark";
            
            console.log("Switching from", currentTheme, "to", newTheme);
            
            document.documentElement.setAttribute("data-bs-theme", newTheme);
            localStorage.setItem("theme", newTheme);
            
            button.textContent = newTheme === "dark" ? "☀️" : "🌙";
            console.log("Theme switched!");
          });
        }
        
        // Initialize theme
        const savedTheme = localStorage.getItem("theme") || "light";
        console.log("Initializing with theme:", savedTheme);
        document.documentElement.setAttribute("data-bs-theme", savedTheme);
        
        const switcher = document.querySelector(".theme-switcher");
        if (switcher) {
          switcher.textContent = savedTheme === "dark" ? "☀️" : "🌙";
        }
      });
    </script>
  </head>

  <body>
    <!-- Theme Switcher -->
    <button class="btn btn-primary theme-switcher" title="Toggle theme" style="position: fixed; top: 20px; right: 20px; z-index: 1050; border-radius: 50%; width: 60px; height: 60px; font-size: 1.5rem;">
      🌙
    </button>

    <%= render "layouts/header" %>
    
    <div class="container-fluid">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    </div>

    <nav class="navbar navbar-expand-lg mb-4">
      <div class="container">
        <%= link_to "Home", root_path, class: "btn btn-primary me-2" %>
        <% if authenticated? %>
          <%= button_to "Log out", session_path, method: :delete, class: "btn btn-outline-danger" %>
        <% else %>
          <%= link_to "Login", new_session_path, class: "btn btn-outline-primary" %>
        <% end %>
      </div>
    </nav>


    <%= yield %>
    <%= render "layouts/footer" %>
  </body>
</html>
